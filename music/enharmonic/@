all:

@@	:=fa.musicxml so.musicxml
@@.xml	:=$(@@:%.musicxml=%.xml)
@@.png	:=$(@@:%.musicxml=%.png)
@@.mp3	:=$(@@:%.musicxml=%.mp3)
@@.*	:=$(@@.png) $(@@.mp3)

$(@@.xml): %.xml: %.musicxml
	cp $< $@

$(@@.*): %.mscz
	open -a 'MuseScore 4' $<
